{
  "name": "valosagresei-shop",
  "version": "0.1.0",
  "private": true,
  "description": "Valóság Résein — Stripe checkout + biztonságos ebook letöltés (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
